import React from 'react';

const ValidationMessages = () => {
    return (
        <div>
            {/* This is where Validation Messages will be displayed */}
        </div>
    );
};

export default ValidationMessages;
