import React from 'react';

const ChatWindow = () => {
    return (
        <div>
            {/* This is where the chat output will be displayed */}
        </div>
    );
};

export default ChatWindow;
