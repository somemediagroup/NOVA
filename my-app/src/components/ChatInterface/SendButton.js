import React from 'react';

const SendButton = () => {
    return (
        <button>
            {/* This is where the "Send" button for chat will go */}
        </button>
    );
};

export default SendButton;
