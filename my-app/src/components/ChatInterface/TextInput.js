import React from 'react';

const TextInput = () => {
    return (
        <input type="text">
            {/* This is where the text input for chat will go */}
        </input>
    );
};

export default TextInput;
