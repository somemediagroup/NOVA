import React from 'react';

const RecentActivity = () => {
    return (
        <div>
            {/* This is where the recent activity will be displayed */}
        </div>
    );
};

export default RecentActivity;
