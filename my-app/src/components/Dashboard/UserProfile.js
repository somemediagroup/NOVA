import React from 'react';

const UserProfile = () => {
    return (
        <div>
            {/* This is where the user profile will be displayed */}
        </div>
    );
};

export default UserProfile;
