import React from 'react';

const EndTimePicker = () => {
    return (
        <div>
            {/* This is where the end time picker will go */}
        </div>
    );
};

export default EndTimePicker;
