import React from 'react';

const FailedToScheduleMessage = () => {
    return (
        <div>
            {/* This is where the "Failed to Schedule" message will go */}
        </div>
    );
};

export default FailedToScheduleMessage;
