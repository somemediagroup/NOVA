import React from 'react';

const StartTimePicker = () => {
    return (
        <div>
            {/* This is where the start time picker will go */}
        </div>
    );
};

export default StartTimePicker;
