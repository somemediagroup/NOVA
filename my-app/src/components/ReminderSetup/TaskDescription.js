import React from 'react';

const TaskDescription = () => {
    return (
        <div>
            {/* This is where the task description input will go */}
        </div>
    );
};

export default TaskDescription;
