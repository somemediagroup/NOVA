import React from 'react';

const InvalidInputMessage = () => {
    return (
        <div>
            {/* This is where the invalid input message will be displayed */}
        </div>
    );
};

export default InvalidInputMessage;
