import React from 'react';

const SearchBar = () => {
    return (
        <input type="text">
            {/* This is where the search bar will go */}
        </input>
    );
};

export default SearchBar;
