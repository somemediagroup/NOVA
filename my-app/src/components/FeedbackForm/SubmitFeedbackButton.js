import React from 'react';

const SubmitFeedbackButton = () => {
    return (
        <button>
            {/* This is where the "Submit Feedback" button will go */}
        </button>
    );
};

export default SubmitFeedbackButton;
