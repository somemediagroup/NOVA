import React from 'react';

const TextArea = () => {
    return (
        <textarea>
            {/* This is where the text area for feedback will go */}
        </textarea>
    );
};

export default TextArea;
